import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sistegeo-apk',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
